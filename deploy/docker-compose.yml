services:
  static:
    container_name: blog.surister.dev
    image: nginx
    restart: unless-stopped
    networks:
      - proxy
    ports:
      - 80
    environment:
      - VIRTUAL_HOST=blog.surister.dev
      - NGINX_HOST=blog.surister.dev
    volumes:
      - ./dist/:/usr/share/nginx/html

networks:
  proxy:
    name: proxy
    external: true
